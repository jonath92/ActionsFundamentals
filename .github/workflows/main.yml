name: add-annotations

on: 
  pull_request: 

jobs: 
  my_job: 
    runs-on: "ubuntu-22.04"
    steps:
      - uses: actions/checkout@v4
      - run: | 
          echo "::notice file=CheatSheet.md,line=1,title=Notice::Formatting issue"
      - run: |
          echo "::warning file=CheatSheet.md,line=2,endLine=5,title=Warning::Linting error"
      - run: |
          echo "::error file=CheatSheet.md,line=7,col=1;title=Error, endColumn=5::Error: Invalid syntax"    

